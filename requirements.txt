# Python dependencies for the cyclepapa multibagger-database pipeline.
# The web container is re-cloned fresh on restart, so these must be
# reinstalled each session — the session-start hook does this.
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
financedatabase>=2.4
yfinance>=1.4
requests>=2.28
