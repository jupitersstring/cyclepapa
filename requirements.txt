# Core analytics + data stack for the earnings_model package.
# yfinance's API (get_earnings_dates, auto_adjust semantics) breaks between
# minors; pin to the tested 1.4.x band rather than an open lower bound.
yfinance>=1.4,<1.5
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
scipy>=1.11
pyarrow>=14.0
curl_cffi>=0.7          # browser-impersonating session; defeats Yahoo 429s

# financedatabase pulls a broken `odfpy` build requirement transitively, so
# install it WITHOUT deps (its real runtime needs are pandas + requests, above):
#   pip install --no-deps financedatabase
financedatabase>=2.3
requests>=2.31
